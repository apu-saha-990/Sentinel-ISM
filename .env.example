# Copy this to .env and fill in real values. Never commit .env itself.

# sentinel-server connection details (used once v1.1 introduces remote/central record)
SENTINEL_SERVER_HOST=
SENTINEL_SERVER_USER=

# Add other secrets here as they're introduced. Nothing hardcoded in scripts.
